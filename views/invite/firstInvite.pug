extends baseinvi 

block content 
    h1.loader
        .bink
            img.porti__of__img(src="/images/smalport.png", alt="porti__img")
            span Por
            span.grn t
            span.nocaps i
    .invite__main 
        if (invite.imgs[0])
            img.mrg__cover(src=`${invite.imgs[0]}`, alt="cover__pic")
        else 
            img.mrg__cover(src="/images/bg.png" alt="cover__pic")
        .invite__main__sub
            h2.coupleName= invite.fname 
                span  &hearts; 
                    span= invite.sname
            h3.mrg__invi Invitation
            .invite__bod
                h3 Our Story 
                .invite__bod__cont
                    .invite__bod__abt
                        pre= invite.about
                    if (invite.imgs[1])
                        img.imgFull(src=`${invite.imgs[1]}`, loading="lazy", alt="snd__img")
                    else 
                        img.imgFull(src="/images/bg.png")
                if (invite.imgs[2])
                    h3 We keep this love in a Photograph :)
                .invite__bod__pic
                    if (invite.imgs[2])
                        img.imgFull(src=`${invite.imgs[2]}`, loading="lazy", alt="thrd__img")
                    if (invite.imgs[3])
                        img.imgFull(src=`${invite.imgs[3]}`, loading="lazy", alt="forth__img")
                    if (invite.imgs[4])
                        img.imgFull(src=`${invite.imgs[4]}`, loading="lazy", alt="fith__img")
                .invite__bod__details 
                    h4.partyDate= `Date: ${invite.pdate }`
                    h4.partyVenue= `Venue: ${invite.address}` 
                    h4.partyVenue= `Contact: ${invite.phn_no}` 
                    h4.partyTime= `Time: ${invite.ptime}`

    script(src="/js/alert.js") 
    script(src="/js/imgFull.js") 


