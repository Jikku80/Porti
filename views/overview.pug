extends base

block content

    if !user 
        include loginPage
    else 
        include _header
        .login__bod 
            h1= `Namaste! ${user.name}, You are Logged In!`
            a(href="/layouts/porti")
                button.redbtn Go to vPorti 
        
    