.login__form
    .loader
        .out__load
            .inn__load
                .inner__loader
        p.lodtl Lakhey
            span.lodmid Tech
                span.lodend ...
    .login__head 
        a.port_sec_head.hm(href="/" title="home")
            img.porti__off__img(src="/images/smalport.png", alt="porti__img")
            span Por
            span.grn t
            span.nocaps i
        if (!user)
            a.rmtxtdec.crpt(href="/porti-highlights" title="layouts")
                img(src="/images/portal.png", alt="view_port")
                p.name__tag Layouts
        else
            a.rmtxtdec.crpt(href="/porti" title="porti")
                img(src="/images/smalport.png", alt="view_port")
                p.name__tag Porti
    form#loginForm(name="logForm")
        h2 Log into your account 
        .login__form__bod
            .form__sub
                label.form__label(for='email') Email Address
                input#email_add.form__input.frm__id(type='email', name="log_email_add", placeholder='you@example.com', required)
            .form__sub
                label.form__label(for='password') Password 
                input#password.form__input.frm__pwd(type='password', name="log_password", placeholder='********', required, minlength='8')
            button.btn__login.redbtn Login
    
    include auth/_forgotPwd

    include auth/_signup


script(src="/js/alert.js") 
script(src="/js/login.js") 


        